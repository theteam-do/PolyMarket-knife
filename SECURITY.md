# 安全指南

## ⚠️ 重要安全警告

### 私钥管理

1. **永远不要**将私钥提交到版本控制
2. **永远不要**在代码中硬编码私钥
3. 使用环境变量或密钥管理服务存储私钥
4. 定期轮换私钥
5. 如果怀疑私钥泄露，立即转移资金并生成新私钥

### 文件安全

以下文件包含敏感信息，**不应提交**到 git：

- `.env`
- `.env.testnet`
- `.env.mainnet`
- `config/*.toml` (包含私钥的配置)
- `*.pem`, `*.key` (密钥文件)

### 配置示例

```bash
# 1. 复制示例配置
cp .env.example .env

# 2. 编辑配置填入真实值
nano .env

# 3. 设置正确的文件权限
chmod 600 .env
```

## 安全最佳实践

### 1. 访问控制

- 使用只读 API 密钥进行监控
- 使用完整权限 API 密钥仅用于交易
- 实施 IP 白名单（如果支持）
- 定期审查 API 权限

### 2. 密钥轮换

```bash
# 建议的轮换周期
# - 测试网：每月
# - 主网：每周
# - 高价值账户：每天
```

### 3. 监控告警

设置以下告警：
- 异常大额交易
- 日亏损超过阈值
- API 调用失败率异常
- 系统资源使用异常

### 4. 应急响应

#### 私钥泄露

1. 立即转移所有资金到新钱包
2. 撤销所有 API 密钥
3. 生成新私钥和 API 密钥
4. 更新所有配置文件
5. 审查交易历史寻找异常

#### 系统被入侵

1. 立即停止所有交易服务
2. 取消所有挂单
3. 断开服务器连接
4. 审查日志寻找入侵痕迹
5. 重新部署干净系统

### 5. 部署安全

```bash
# 生产环境部署检查清单
# [ ] 使用专用服务器/VPS
# [ ] 启用防火墙
# [ ] 禁用 root SSH 登录
# [ ] 使用 SSH 密钥认证
# [ ] 安装安全更新
# [ ] 配置日志审计
# [ ] 设置监控告警
# [ ] 备份私钥（加密存储）
```

## 漏洞报告

如发现安全漏洞，请：

1. **不要**公开披露
2. 发送邮件至：security@polymarket.com
3. 包含详细描述和复现步骤
4. 允许 48 小时响应时间

## 依赖安全

定期检查依赖漏洞：

```bash
# 安装 cargo-audit
cargo install cargo-audit

# 运行安全审计
cargo audit

# 自动修复（如果可能）
cargo audit fix
```

## 审计日志

所有交易操作都会记录到日志：

```bash
# 查看日志
tail -f logs/*.log

# 搜索特定操作
grep "order_placed" logs/*.log
```

## 联系

安全问题联系：security@polymarket.com

---

**最后更新**: 2026-03-05
